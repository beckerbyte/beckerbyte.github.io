{
  "name": "beckerbyte-portfolio",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "frames": "./scripts/generate-frames.sh",
    "check": "npm run build && node scripts/check.mjs"
  }
}
